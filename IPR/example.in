#####list paths for LAMMPS executable and ipr location####
lammps_exe YOUR-LAMMPS-PATH
#ipr_dir 

####define which potentials to use####
#potential +
#    and
#           all (runs all potentials in ref/potentialInstances-LAMMPS)
#    or
#           name NAME1 NAME2 NAME3 ... (runs potentials listed)
#    or
#           element El E2 E3 ... (runs any potentials with those elements)   
#Examples: 
potential all
potential name Ag--AcklandGJ--1987 Ag--AdamsJB--1989 Ag--WilliamsPL--2006 Ag--ZhouXW--2004
potential element Fe W 

####run 0K structure analysis####
#run_0K_structure +
#  and/not
#                 prototypes + (optional. specifies which prototypes to use)
#                            all (run all in ref/crystalPrototypes, which is default)
#                     or
#                            name NAME1 NAME2 ... (runs prototypes listed)
#  and/or/nor
#                 r_range VALUE VALUE VALUE (optional entry for setting the minimum, maximum  
#                                            and number of steps used for nearest neighbor 
#                                            distance, r, in the calculation.
#                                            default is 2.0 5.0 200) 
#Examples:
run_0K_structure
run_0K_structure prototypes all
run_0K_structure r_range 2.0 5.0 100
run_0K_structure prototypes name bcc fcc A15 r_range 2.2 6.0 300
run_0K_structure prototypes all r_range 2.0 5.0 100
run_0K_structure r_range 2.0 5.0 100 prototypes all

potential element Fe W 
####Define preferred crystal to investigate####
#crystal +
#   and
#        clear (clears the list of crystals)
#   or
#        add + (adds a crystal to the list to be run)
#            prototype NAME + (specify the crystal prototype)
#                   and 
#                           elements E1 E2 ... (list element-site mapping for crystal)
#Examples:
crystal clear
crystal add prototype fcc elements Ag
crystal add prototype NaCl elements Ag Cu

####run pressure dependent elastic constant analysis####
#run_cij_vs_P +
#  and/not
#             strain VALUE (optional. set the total strain range to explore, default is 0.1)
#  and/or/nor 
#             steps VALUE (optional. set the number of steps to plot, default is 200)
#Examples:
run_cij_vs_P
run_cij_vs_P strain 0.05 steps 400

####run point defect analysis####
#run_point_defect +
#    and/not
#                 types + (optional. Specifies which types of point defects to use)
#                       all (run all defects in library for given prototype, which is default)
#                   or
#                       list NAME1 NAME2 ... (list specific defects
#    and/or/nor
#                 size_range VALUE VALUE (optional. Defines system size range to use. Default is 4 10)
#Examples:
run_point_defect
run_point_defect types vac 111
run_point_defect size_range 6 14
run_point_defect types 1nn 2nn size_range 4 12

####run dislocation monopole analysis####
#run_dislocation + 
#   and/not        
#                  size VALUE VALUE VALUE  (optional. dimension scalers for the system. Default is 40 40 1)
#   and/or/nor
#                  temperature VALUE (optional. sets the temperature for relaxation. Default is 0)
#   and/or/nor
#                  xwidth VALUE VALUE (optional. sets the range of x values in A to include in the plot windows. Default is -10 10)
#   and/or/nor
#                  ywidth VALUE VALUE (optional. sets the range of y values in A to include in the plot windows. Default is -10 10)
#Examples:
run_dislocation
run_dislocation size 80 80 2
run_dislocation temperature 100
run_dislocation xwidth -15 15 ywidth -15 15
run_dislocation temperature 50 size 60 60 1 xwidth -20 20 ywidth -20 20

